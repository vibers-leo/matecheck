# 🚀 MateCheck 앱 배포 가이드 (iOS & Android)

이 가이드는 Expo(EAS)를 사용하여 MateCheck 앱을 애플 앱스토어와 구글 플레이 스토어에 배포하는 전체 과정을 설명합니다.

---

## 0. 📋 사전 준비 (필수)

가장 먼저 각 스토어의 개발자 계정이 있어야 합니다. (비용 발생)

1.  **Apple Developer Program**
    *   비용: 연 $99 (매년 갱신)
    *   가입: [developer.apple.com](https://developer.apple.com/)
    *   *필요한 것: D-U-N-S 번호(사업자일 경우), Apple 기기(2단계 인증용)*

2.  **Google Play Console**
    *   비용: $25 (평생 1회 결제)
    *   가입: [play.google.com/console](https://play.google.com/console)

---

## 1. ⚙️ 프로젝트 설정 (app.json)

배포 전에 앱의 고유한 신분증을 만들어야 합니다. `app.json` 파일에서 다음 내용을 확인하고 수정하세요.
**주의:** `bundleIdentifier`와 `package` 이름은 전 세계에서 유일해야 합니다.

```json
{
  "expo": {
    "name": "MateCheck",
    "slug": "matecheck",
    "version": "1.0.0",
    "ios": {
      "supportsTablet": true, 
      "bundleIdentifier": "com.juuuno.matecheck"  // ✨ 수정 필요: 고유한 ID로 변경 (예: com.내이름.matecheck)
    },
    "android": {
      "package": "com.juuuno.matecheck",           // ✨ 수정 필요: ios와 동일하게 맞추는 것 권장
      "versionCode": 1,
      "adaptiveIcon": { ... }
    },
    "extra": {
      "eas": {
        "projectId": "..." // EAS 설정 시 자동 생성됨
      }
    }
  }
}
```

---

## 2. 🛠 EAS(Expo Application Services) 설치 및 설정

터미널에서 다음 명령어를 순서대로 실행합니다.

### 2-1. EAS CLI 설치 및 로그인
```bash
npm install -g eas-cli
eas login
# Expo 계정으로 로그인 (없으면 expo.dev에서 가입)
```

### 2-2. 프로젝트 구성
```bash
eas build:configure
# 플랫폼 선택: All (iOS + Android)
```
이 과정이 끝나면 `eas.json` 파일이 생성됩니다.

---

## 3. 🏗️ 앱 빌드하기 (클라우드 빌드)

이제 Expo 클라우드 서버에서 앱 설치 파일을 만듭니다.

### 3-1. Android 빌드 (AAB 파일)
구글 플레이 스토어는 `.aab` (App Bundle) 형식을 요구합니다.
```bash
eas build --platform android --profile production
```
*   빌드가 완료되면 다운로드 링크가 나옵니다. 이 `.aab` 파일을 잘 보관하세요.

### 3-2. iOS 빌드 (IPA 파일)
애플 앱스토어용 `.ipa` 파일을 만듭니다.
```bash
eas build --platform ios --profile production
```
*   *첫 빌드 시 Apple 계정 로그인을 요청하며, 인증서(Certificate)와 프로비저닝 프로필을 자동으로 생성해줍니다. (매우 편함!)*

---

## 4. 🚀 스토어 제출 (업로드)

### 4-1. Android (Google Play Console)
1.  **Google Play Console** 접속 > **앱 만들기** 클릭.
2.  앱 정보를 입력하고 **대시보드**로 이동.
3.  **테스트 > 내부 테스트** (또는 비공개 테스트) 메뉴 선택.
4.  **새 버전 만들기**를 누르고, 위에서 다운로드 받은 `.aab` 파일을 업로드합니다.
5.  버전 검토 및 출시 시작.

### 4-2. iOS (App Store Connect)
iOS는 `EAS Submit` 기능을 쓰면 매우 편합니다. (Mac이 없어도 가능)

1.  **App Store Connect** ([appstoreconnect.apple.com](https://appstoreconnect.apple.com)) 접속 > **나의 앱** > **(+) 신규 앱**.
2.  앱 정보를 생성합니다 (Bundle ID는 `app.json`과 일치해야 함).
3.  터미널에서 다음 명령어 실행:
    ```bash
    eas submit --platform ios
    ```
4.  빌드된 앱을 선택하면 자동으로 Apple 서버로 전송됩니다.
5.  업로드 완료 후 **TestFlight** 탭에서 처리 상태를 확인합니다.

---

## 5. ✅ 심사 및 출시

1.  **스크린샷 준비**: 앱의 주요 기능을 캡처하여 스토어 양식에 맞게 업로드.
2.  **개인정보 처리방침**: 노션이나 블로그 등에 작성하고 링크 등록.
3.  **심사 제출**: 모든 정보를 입력하고 '검토 요청'을 누릅니다.
    *   심사 기간: 보통 1~3일 소요 (반려될 경우 수정 후 재제출)

---

## 💡 꿀팁

*   **아이콘 & 스플래시**: `assets/icon.png` (1024x1024), `assets/splash.png` 이미지를 고화질로 꼭 교체하세요. 앱의 첫인상입니다.
*   **버전 올리기**: 업데이트할 때는 `version` (1.0.0 -> 1.0.1)과 `versionCode`(Android), `buildNumber`(iOS)를 반드시 올려야 합니다.
